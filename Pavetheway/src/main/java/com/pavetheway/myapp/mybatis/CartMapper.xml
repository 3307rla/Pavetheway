<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cart"> 
	<insert id="insert" parameterType="cartDto">
		INSERT INTO cart
		(num, id, code, amount) 
		VALUES ( seq.cart.NEXTVAL, #{id}, #{code}, #{amount})
	</insert>

	<select id="getList" resultType="cartDto">
        SELECT c.num, s.id, code, p.name, amount, price, (price*amount) money
        FROM users u, cart c, shop_list s
        WHERE u.id=c.id and c.code=s.code and c.id=#{id}
        ORDER BY num   
    </select>
</mapper>




	